apiVersion: kargo.akuity.io/v1alpha1
kind: PromotionTask
metadata:
  name: promote-frontend
  namespace: aks-store
spec:
  vars:
    - name: gitRepo
      value: https://github.com/chriskeAlerant/kargo-demo-gitops.git
    - name: targetBranch
      value: main
    - name: storeFrontImageRepo
      value: ghcr.io/chriskealerant/kargo-demo-store-front
  steps:
    - uses: git-clone
      as: clone-frontend
      if: ${{ ctx.targetFreight.origin.name == "frontend" }}
      config:
        repoURL: ${{ vars.gitRepo }}
        checkout:
          - branch: ${{ vars.targetBranch }}
            path: ./repo
        author:
          name: kargo
          email: kargo@example.invalid

    - uses: yaml-update
      as: update-frontend-tag
      if: ${{ ctx.targetFreight.origin.name == "frontend" }}
      config:
        path: ./repo/environments/${{ vars.targetEnv }}/values.yaml
        updates:
          - key: images.storeFront.tag
            value: ${{ imageFrom(vars.storeFrontImageRepo, warehouse('frontend')).Tag }}

    - uses: git-commit
      as: commit-frontend
      if: ${{ ctx.targetFreight.origin.name == "frontend" }}
      config:
        path: ./repo
        message: |
          promote frontend ${{ imageFrom(vars.storeFrontImageRepo, warehouse('frontend')).Tag }} to ${{ vars.targetEnv }}

          ${{ task.outputs['update-frontend-tag'].commitMessage }}

    - uses: git-push
      as: push-frontend
      if: ${{ ctx.targetFreight.origin.name == "frontend" }}
      config:
        path: ./repo
        targetBranch: ${{ vars.targetBranch }}

    - uses: argocd-update
      as: refresh-frontend-argocd
      if: ${{ ctx.targetFreight.origin.name == "frontend" }}
      config:
        apps:
          - name: ${{ vars.argocdApp }}
            sources:
              - repoURL: ${{ vars.gitRepo }}
                desiredRevision: ${{ task.outputs['commit-frontend'].commit }}
                updateTargetRevision: false
---
apiVersion: kargo.akuity.io/v1alpha1
kind: PromotionTask
metadata:
  name: promote-backend-bundle
  namespace: aks-store
spec:
  vars:
    - name: gitRepo
      value: https://github.com/chriskeAlerant/kargo-demo-gitops.git
    - name: targetBranch
      value: main
    - name: productServiceImageRepo
      value: ghcr.io/chriskealerant/kargo-demo-product-service
    - name: orderServiceImageRepo
      value: ghcr.io/chriskealerant/kargo-demo-order-service
  steps:
    - uses: git-clone
      as: clone-backend
      if: ${{ ctx.targetFreight.origin.name == "backend-bundle" }}
      config:
        repoURL: ${{ vars.gitRepo }}
        checkout:
          - branch: ${{ vars.targetBranch }}
            path: ./repo
        author:
          name: kargo
          email: kargo@example.invalid

    - uses: yaml-update
      as: update-backend-tags
      if: ${{ ctx.targetFreight.origin.name == "backend-bundle" }}
      config:
        path: ./repo/environments/${{ vars.targetEnv }}/values.yaml
        updates:
          - key: images.productService.tag
            value: ${{ imageFrom(vars.productServiceImageRepo, warehouse('backend-bundle')).Tag }}
          - key: images.orderService.tag
            value: ${{ imageFrom(vars.orderServiceImageRepo, warehouse('backend-bundle')).Tag }}

    - uses: git-commit
      as: commit-backend
      if: ${{ ctx.targetFreight.origin.name == "backend-bundle" }}
      config:
        path: ./repo
        message: |
          promote backend bundle ${{ imageFrom(vars.productServiceImageRepo, warehouse('backend-bundle')).Tag }} to ${{ vars.targetEnv }}

          ${{ task.outputs['update-backend-tags'].commitMessage }}

    - uses: git-push
      as: push-backend
      if: ${{ ctx.targetFreight.origin.name == "backend-bundle" }}
      config:
        path: ./repo
        targetBranch: ${{ vars.targetBranch }}

    - uses: argocd-update
      as: refresh-backend-argocd
      if: ${{ ctx.targetFreight.origin.name == "backend-bundle" }}
      config:
        apps:
          - name: ${{ vars.argocdApp }}
            sources:
              - repoURL: ${{ vars.gitRepo }}
                desiredRevision: ${{ task.outputs['commit-backend'].commit }}
                updateTargetRevision: false
